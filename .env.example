# Optional local env — copy to .env.local. Never commit real keys.

# If set, the map can overlay a Google embed. Without it, the crafted SVG India
# rail map is the demo. Do not put production credentials here.
# NEXT_PUBLIC_GOOGLE_MAPS_KEY=

# Google Earth Engine is not used. Do not add GEE keys.
